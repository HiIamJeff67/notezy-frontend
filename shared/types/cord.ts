export type Cord = [number, number];
export type ThreeCord = [number, number, number];

export type FrameCountPosition = {
  leftFrameCount: number;
  topFrameCount: number;
};

export type FrameCountSize = {
  widthFrameCount: number;
  heightFrameCount: number;
};

export type Position = {
  left: number;
  top: number;
};

export type Size = {
  width: number;
  height: number;
};
