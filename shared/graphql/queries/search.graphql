query SearchUsers($input: SearchUserInput!) {
  searchUsers(input: $input) {
    searchEdges {
      encodedSearchCursor
      node {
        ...FragmentedPublicUser
      }
    }
    searchPageInfo {
      hasNextPage
      hasPreviousPage
      startEncodedSearchCursor
      endEncodedSearchCursor
    }
    totalCount
    searchTime
  }
}

query SearchThemes($input: SearchThemeInput!) {
  searchThemes(input: $input) {
    searchEdges {
      encodedSearchCursor
      node {
        ...FragmentedPublicTheme
      }
    }
    searchPageInfo {
      hasNextPage
      hasPreviousPage
      startEncodedSearchCursor
      endEncodedSearchCursor
    }
    totalCount
    searchTime
  }
}

query SearchRootShelves($input: SearchRootShelfInput!) {
  searchRootShelves(input: $input) {
    searchEdges {
      node {
        ...FragmentedPrivateRootShelf
      }
      encodedSearchCursor
    }
    searchPageInfo {
      hasNextPage
      hasPreviousPage
      startEncodedSearchCursor
      endEncodedSearchCursor
    }
    totalCount
    searchTime
  }
}

query SearchSubShelves($input: SearchSubShelfInput!) {
  searchSubShelves(input: $input) {
    searchEdges {
      node {
        ...FragmentedPrivateSubShelf
      }
      encodedSearchCursor
    }
    searchPageInfo {
      hasNextPage
      hasPreviousPage
      startEncodedSearchCursor
      endEncodedSearchCursor
    }
    totalCount
    searchTime 
  }
}

query SearchItems($input: SearchItemInput!) {
  searchItems(input: $input) {
    searchEdges {
      node {
        ...FragmentedPrivateItem
      }
      encodedSearchCursor
    }
    searchPageInfo {
      hasNextPage
      hasPreviousPage
      startEncodedSearchCursor
      endEncodedSearchCursor
    }
    totalCount
    searchTime
  }
}

query SearchBlocks($input: SearchBlockInput!) {
  searchBlocks(input: $input) {
    searchEdges {
      node {
        ...FragmentedPrivateBlock
      }
      encodedSearchCursor
    }
    searchPageInfo {
      hasNextPage
      hasPreviousPage
      startEncodedSearchCursor
      endEncodedSearchCursor
    }
    totalCount
    searchTime
  }
}

query SearchStations($input: SearchStationInput!) {
  searchStations(input: $input) {
    searchEdges {
      node {
        ...FragmentedPrivateSearchableStation
      }
      encodedSearchCursor
    }
    searchPageInfo {
      hasNextPage
      hasPreviousPage
      startEncodedSearchCursor
      endEncodedSearchCursor
    }
    totalCount
    searchTime
  }
}

query SearchRoutines($input: SearchRoutineInput!) {
  searchRoutines(input: $input) {
    searchEdges {
      node {
        ...FragmentedPrivateSearchableRoutine
      }
      encodedSearchCursor
    }
    searchPageInfo {
      hasNextPage
      hasPreviousPage
      startEncodedSearchCursor
      endEncodedSearchCursor
    }
    totalCount
    searchTime
  }
}

query SearchRoutineTags($input: SearchRoutineTagInput!) {
  searchRoutineTags(input: $input) {
    searchEdges {
      node {
        ...FragmentedPrivateRoutineTag
      }
      encodedSearchCursor
    }
    searchPageInfo {
      hasNextPage
      hasPreviousPage
      startEncodedSearchCursor
      endEncodedSearchCursor
    }
    totalCount
    searchTime
  }
}

query SearchRoutineTasks($input: SearchRoutineTaskInput!) {
  searchRoutineTasks(input: $input) {
    searchEdges {
      node {
        ...FragmentedPrivateRoutineTask
      }
      encodedSearchCursor
    }
    searchPageInfo {
      hasNextPage
      hasPreviousPage
      startEncodedSearchCursor
      endEncodedSearchCursor
    }
    totalCount
    searchTime
  }
}
