type PublicUserInfo {
  # id: UUID!
  # userId: UUID!
  avatarURL: String
  coverBackgroundURL: String
  header: String
  introduction: String
  gender: UserGender!
  country: Country
  birthDate: Time!
  # updatedAt: Time!
}
