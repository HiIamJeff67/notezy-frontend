type PrivateSubShelf {
  id: UUID!
  name: String!
  rootShelfId: UUID!
  prevSubShelfId: UUID
  path: [UUID!]!
  deletedAt: Time
  updatedAt: Time!
  createdAt: Time!

  # relations
  nextSubShelfIds: [UUID!]!
  itemIds: [UUID!]!
}
