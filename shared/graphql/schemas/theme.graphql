type PublicTheme {
  # id: UUID!
  publicId: UUID! # the encoded of this field is the identifier of the PublicTheme
  name: String!
  isDark: Boolean!
  authorId: UUID!
  version: String!
  isDefault: Boolean!
  downloadURL: String
  downloadCount: Int64!
  createdAt: Time!
  updatedAt: Time!
}
